import 'package:flutter/material.dart';

class AppColors {
  // Dark them

  // Common
  static const commonAccent = Colors.amber;
  static const commonButtonText = Color.fromARGB(255, 42, 15, 165);
  static const primaryTextDark = Colors.white;
  static const primaryTextLight = Color(0xFF1F2937);
  static const secondaryText = Color(0xFF9CA3AF);
}
